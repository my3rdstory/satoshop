## 배경
- CTA 이메일/후원 주소 복사 기능에 대한 안내가 없어 복사 여부를 알기 어려웠음.

## 변경 사항
- 복사 클릭 시 버튼 텍스트를 2초간 "복사되었습니다"로 표시한 뒤 원문으로 복원.
- 안내 상태에서 시각적 강조 스타일을 추가.

## 영향
- CTA 복사 동작에 대한 즉시 피드백이 제공됨.

## 대장 수동 테스트 가이드
1. `/minihome/<slug>/`에서 CTA 이메일/후원 주소 클릭.
2. 버튼 텍스트가 2초간 "복사되었습니다"로 바뀌었다가 원문으로 복원되는지 확인.
